import { defineConfig } from 'vite'

export default defineConfig({
  // ← Add this line (replace "Chess" with your exact repo name if different)
  base: '/Chess/',

  test: {
    coverage: {
      provider: 'v8',
      include: ['src/**/*.{js,ts}'],
      exclude: ['src/pgn.js'],
    },
  },
})